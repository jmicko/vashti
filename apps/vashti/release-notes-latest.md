# Unreleased

Track user-facing changes here while working on the next Vashti release.

- Notes access now lives in Personal → Notes settings with clearer model-action and new-note defaults.
- The Notes chat toggle always remains visible and explains any setting that prevents it from working.
- New notes default to full model access, while existing notes keep their current per-note access.
- Model-created server and device notes always remain usable by their creating model.

Before publishing, either leave this file in place and let `publish-release.sh` use it, or copy it
to `apps/vashti/release-notes/vX.Y.Z.md` once the version number is final.
