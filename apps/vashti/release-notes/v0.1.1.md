# v0.1.1

* Added an app version display to App Settings so installs and updates are easy to verify.
* Improved the release publishing script with hidden one-time upload key input and cleaner output.
* Fixed service installers so updates restart the running service after replacing the binary.
