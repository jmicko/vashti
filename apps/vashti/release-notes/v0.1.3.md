# v0.1.3

* Replaced the app logo asset with a smaller transparent version so the neon logo loads faster and blends into dark backgrounds.
* Added a separate smaller favicon for the browser tab.
* Improved install output so new users see concrete local, LAN, and VPN URLs to try after setup.
